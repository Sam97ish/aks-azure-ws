apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azuredisk-configdb-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-premium 
  resources:
    requests:
      storage: 5Gi